{% extends 'base.html.twig' %}

{% block title %}Erreur {{ status_code }} - Chateau de Belleville{% endblock %}

{% block header %}{% endblock %}

{% block body %}
    <section style="min-height: 60vh; display: flex; align-items: center; justify-content: center; background: var(--color-cream); padding: 4rem 5%;">
        <div style="text-align: center; max-width: 560px;">
            <div style="font-size: 5rem; margin-bottom: 1.5rem;">🍇</div>
            <h1 style="font-family: var(--font-display); font-size: 3.5rem; color: var(--color-primary); margin-bottom: 0.5rem;">{{ status_code }}</h1>
            <h2 style="font-family: var(--font-display); font-size: 1.8rem; color: var(--color-primary); margin-bottom: 1.5rem;">{{ status_text }}</h2>
            <p style="color: var(--color-secondary); line-height: 1.8; margin-bottom: 2.5rem;">
                Une erreur est survenue lors du traitement de votre demande.
                <br>Veuillez reessayer ou retourner a l'accueil.
            </p>
            <a href="{{ path('app_home') }}" class="btn btn-primary">Retour a l'accueil</a>
        </div>
    </section>
{% endblock %}
